Files to encapsulate the  commands users will need to run to export their data from Sat5

===
$ ./stargate-export --help
Usage: stargate-export [options]

Options:
  -h, --help            show this help message and exit
  -x DIR, --export-dir=DIR
                        Specify directory to store exports in (will be created
                        if not found)
  -p FILE, --export-package=FILE
                        Specify filename to use for final packaged-exports
                        tarfile
  -e entity[,entity...], --entities=entity[,entity...]
                        Specify comma-separated list of entities to export
                        (default is all)
  -l, --list-entities   List supported entities
  -d, --debug           Log debugging output
  -q, --quiet           Log only errors
===
