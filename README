To enable modules do following:

    mkdir -p ~/.hammer
    cat > ~/.hammer/cli_config.yml << EOF
    :modules:
      - hammer_cli_import
EOF

And running with

    # env RUBYOPT=-Ilib hammer zzzexample

